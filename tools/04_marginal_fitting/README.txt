Environment: conda activate synpop 

Workflow:
 - the notebooks are developed in project
 - final versions are copy over as generic tools to SynPopToolbox
 - these notebooks are committed and pushed (even if using git with notebooks is not great, it is good to have it versioned in some way)

How to use these notebooks:
 - copy them over to the project and adapt them there
 - change the path to the SynPopToolbox package and the global settings
 - if a significant improvement has been made added, the versioned notebook should be updated as well
